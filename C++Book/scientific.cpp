#include <iostream>
using namespace std;
// For 1. the value of i is 0, Since a float cannot be converted to an interger;
//For 3. 42 is a correct integer number in C++
//For 4. 9 is the value of f
//For 6. 4.2 is the correct float in C++
//For 7. The utput is 7 6 6
// For 8. The output is 13 -1
//For 9. 2 is printed by the program
//For 10. f is printed by the program
//For 12. The value is 91211
//For 14. bool is not a valid variable name because it is reserved for the data type
//For 17. this_one_is is the only legal variable name in C++
//For 18. The output is 31
//For 19. E, F, and C are legal interger literal values in C++
// For 20. The output is 20
//For 21. The compilation fails as double == signs are needed.
int main () {
   // float f;
    //cin >> f;
    //cout << scientific << "f";
    //int n;
    //cin >> n;
    //int x = 3, y = x++, z = ++x;
   // cin >> x;
   // cout << z - x << y - x <<  endl;
   //int x = 3, y = x++, z = ++x;
    //cin >> x;
   // cout << z - x << y - x <<  endl;
   //int n = 2;
   // cout << (n == "2") <<  endl;
    //cout << n << hex << n + n << oct << n << endl;
     //int i = f; 
     //int i = 5, j = i++, k = ++i;
     //cin >> i;
     //cout << j + i << k - i << endl;
     //int i = 5, j = ++i, k = i++;
     //cout << i << j << k << endl;
    //cout << f;
    //cout << scientific << f << "\n";
    /*int i = 5, j, k;
    if (i > 0) j = 2 + i * i;
    if (i <= 0) j = 2 * i - 1;
    if (j >= 0) k = j % i + 2;
    if (j < 0) k = i % j + 2;
    if (k < 0) k = k % i % j;
    if (k >= 0) k = j % i % k;
    cout << k << endl;*/
    return 0;
}